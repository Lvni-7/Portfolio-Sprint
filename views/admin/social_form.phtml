<div class="admin-form-container">
    <div class="admin-header">
        <h1><?= $social ? 'Modifier' : 'Ajouter' ?> un Réseau Social</h1>
        <a href="index.php?action=social-list" class="btn btn-secondary">Retour</a>
    </div>

    <form action="index.php?action=social-save" method="post" class="admin-form">
        <?php if ($social): ?>
            <input type="hidden" name="id" value="<?= (int)$social->getId() ?>">
        <?php endif; ?>

        <div class="form-main">
            <div class="form-group">
                <label for="name">Nom du réseau</label>
                <input type="text" name="name" id="name" value="<?= $social ? htmlspecialchars($social->getName()) : '' ?>" placeholder="Ex: GitHub, LinkedIn..." required>
            </div>

            <div class="form-group">
                <label for="url">URL du profil</label>
                <input type="url" name="url" id="url" value="<?= $social ? htmlspecialchars($social->getUrl()) : '' ?>" placeholder="https://..." required>
            </div>

            <div class="form-group">
                <label for="icon">Icône (Lucide)</label>
                <input type="text" name="icon" id="icon" value="<?= $social ? htmlspecialchars($social->getIcon()) : 'link' ?>" placeholder="Ex: github, linkedin, mail...">
            </div>

            <div class="form-actions">
                <button type="submit" class="btn btn-primary"><?= $social ? 'Enregistrer' : 'Créer' ?></button>
            </div>
        </div>
        
        </div>
    </form>
</div>
