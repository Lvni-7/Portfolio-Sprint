<div class="admin-header">
    <h1>Gestion des Catégories</h1>
    <div class="header-actions">
        <a href="index.php?action=admin-dashboard" class="btn">Retour Dashboard</a>
        <a href="index.php?action=category-form" class="btn btn-success">Ajouter une catégorie</a>
    </div>
</div>

<table class="admin-table">
    <thead>
        <tr>
            <th>ID</th>
            <th>Nom</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        <?php foreach ($categories as $category): ?>
            <tr>
                <td><?= (int)$category->getId() ?></td>
                <td><?= htmlspecialchars($category->getName()) ?></td>
                <td>
                    <a href="index.php?action=category-form&id=<?= (int)$category->getId() ?>" class="btn btn-sm">Modifier</a>
                    <a href="index.php?action=category-delete&id=<?= (int)$category->getId() ?>" class="btn btn-sm btn-danger" onclick="return confirm('Supprimer cette catégorie ? Cela n\'affectera pas les projets associés (ils n\'auront plus de catégorie).')">Supprimer</a>
                </td>
            </tr>
        <?php endforeach; ?>
    </tbody>
</table>
