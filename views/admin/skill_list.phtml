<div class="admin-header">
    <h1>Gestion des Skills</h1>
    <a href="index.php?action=skill-form" class="btn btn-success btn-sm">Ajouter un Skill</a>
</div>

<div class="admin-table-container">
    <table class="admin-table">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Niveau (%)</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($skills as $skill): ?>
                <tr>
                    <td><?= htmlspecialchars($skill->getName()) ?></td>
                    <td>
                        <div class="progress-bar">
                            <div class="progress-fill" style="--progress: <?= (int)$skill->getLevel() ?>%"></div>
                        </div>
                        <small><?= (int)$skill->getLevel() ?>%</small>
                    </td>
                    <td>
                        <div class="card-actions">
                            <a href="index.php?action=skill-form&id=<?= (int)$skill->getId() ?>" class="btn btn-primary btn-sm">Modifier</a>
                            <a href="index.php?action=skill-delete&id=<?= (int)$skill->getId() ?>" 
                               class="btn btn-danger btn-sm" 
                               onclick="return confirm('Supprimer cette compétence ?')">Supprimer</a>
                        </div>
                    </td>
                </tr>
            <?php endforeach; ?>
        </tbody>
    </table>
</div>

<div class="admin-footer-actions">
    <a href="index.php?action=admin-dashboard" class="btn btn-secondary">Retour au Tableau de bord</a>
</div>
