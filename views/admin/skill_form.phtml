<div class="admin-form-container">
    <div class="admin-header">
        <h1><?= $skill ? 'Modifier' : 'Ajouter' ?> un Skill</h1>
        <a href="index.php?action=skill-list" class="btn btn-secondary">Retour</a>
    </div>

    <form action="index.php?action=skill-save" method="post" class="admin-form">
        <?php if ($skill): ?>
            <input type="hidden" name="id" value="<?= (int)$skill->getId() ?>">
        <?php endif; ?>

        <div class="form-main">
            <div class="form-group">
                <label for="name">Nom du Skill</label>
                <input type="text" name="name" id="name" value="<?= $skill ? htmlspecialchars($skill->getName()) : '' ?>" placeholder="Ex: PHP, React, UI..." required>
            </div>

            <div class="form-group">
                <label for="level">Niveau de maîtrise (<span id="level-val"><?= $skill ? (int)$skill->getLevel() : '50' ?></span>%)</label>
                <input type="range" name="level" id="level" min="0" max="100" value="<?= $skill ? (int)$skill->getLevel() : '50' ?>" oninput="document.getElementById('level-val').innerText = this.value">
                <small class="help-text">Déplacez le curseur pour simuler votre niveau d'expertise.</small>
            </div>

            <div class="form-actions">
                <button type="submit" class="btn btn-primary"><?= $skill ? 'Enregistrer' : 'Créer' ?></button>
            </div>
        </div>
        
        </div>
    </form>
</div>
