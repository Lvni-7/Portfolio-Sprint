<div class="admin-header">
    <h1>Réseaux Sociaux</h1>
    <a href="index.php?action=social-form" class="btn btn-primary btn-sm">Ajouter un lien</a>
</div>

<div class="admin-table-container">
    <table class="admin-table">
        <thead>
            <tr>
                <th>Réseau</th>
                <th>URL</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($socials as $social): ?>
                <tr>
                    <td>
                        <span class="social-preview">
                            <span class="social-icon-preview">
                                <i data-lucide="<?= htmlspecialchars($social->getIcon() ?: 'link') ?>"></i>
                            </span>
                            <?= htmlspecialchars($social->getName()) ?>
                        </span>
                    </td>
                    <td><a href="<?= htmlspecialchars($social->getUrl()) ?>" target="_blank" class="url-link"><?= htmlspecialchars($social->getUrl()) ?></a></td>
                    <td>
                        <div class="actions-flex">
                            <a href="index.php?action=social-form&id=<?= (int)$social->getId() ?>" class="btn btn-secondary btn-sm">Modifier</a>
                            <a href="index.php?action=social-delete&id=<?= (int)$social->getId() ?>" 
                               class="btn btn-danger btn-sm" 
                               onclick="return confirm('Supprimer ce lien social ?')">Supprimer</a>
                        </div>
                    </td>
                </tr>
            <?php endforeach; ?>
        </tbody>
    </table>
</div>

<div class="mt-4">
    <a href="index.php?action=admin-dashboard" class="btn btn-secondary">Retour au Tableau de bord</a>
</div>
